# CCTV source fixtures

These files are synthetic/reconstructed fixtures for offline unit tests. They
are based only on the field names and verified public examples recorded in
`docs/cctv-viewer.md`; they contain no live response bodies, credentials, or
provider secrets. Values containing `fixture-secret` are adversarial test
markers and are expected never to cross the source boundary.
